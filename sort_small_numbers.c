#include "header.h"

void	push_half(t_stack *a, t_stack *b)
{
	int	half;
	int	max_half;
	int	max_index;

	max_half = a->n / 2;
	while (a->n > max_half)
	{
		half = a->n / 2;
		max_index = ft_max(a->elements,a->n);
	}
}